version: '3.8'

# Override file for local development
# This builds the snapshot-service locally instead of pulling from registry

services:
  snapshot-service:
    build:
      context: .
      dockerfile: Dockerfile.snapshot-service
    image: creativewriter2-snapshot-service:local
